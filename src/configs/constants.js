const ACCESS_TOKEN = "ACCESS_TOKEN";
const REFRESH_TOKEN = "REFRESH_TOKEN";
const ROLES = {
    ADMIN: 'ADMIN',
    USER: 'USER',
    GUEST: 'GUEST'
}

module.exports = {
    ACCESS_TOKEN,
    REFRESH_TOKEN,
    ROLES
}